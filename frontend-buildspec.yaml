version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 21.6.1
    commands:
      - echo "Install phase"

  build:
    commands:
      - echo "Building frontend"
      - cd ./frontend
      - npm install
      - npm run build

  # post_build:
  #   commands:
  #     - cp -r frontend/out/* .

artifacts:
  files:
    - '**/*'
  base-directory: 'frontend/out'
  # discard-paths: yes
